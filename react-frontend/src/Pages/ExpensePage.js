import React from "react";
import ExpenseForm from "../components/ExpenseForm";

const ExpensePage = () => {
  return (
    <React.Fragment>
      <ExpenseForm />
    </React.Fragment>
  );
};

export default ExpensePage;
